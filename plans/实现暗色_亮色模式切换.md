# 暗色/亮色模式切换功能实现计划

本计划旨在为应用添加明暗主题切换功能，通过 Tailwind CSS 的 `darkMode: 'class'` 策略实现，并对现有 Glassmorphism 风格进行适配。

## 1. 配置 Tailwind CSS
- 修改 `tailwind.config.js`，启用手动切换模式：
  ```javascript
  export default {
    darkMode: 'class',
    // ...
  }
  ```

## 2. 升级全局样式 (src/index.css)
- **重构 Body 样式**：
  - 暗色模式：保持现有的 `bg-slate-950` 和深色渐变。
  - 亮色模式：使用 `bg-slate-50` 和更柔和的紫/绿渐变。
- **升级 Glassmorphism 工具类**：
  - `.glass-panel`: 
    - Dark: `bg-slate-900/40 border-white/10`
    - Light: `bg-white/60 border-slate-200/50 shadow-xl shadow-slate-200/20`
  - `.glass-card`:
    - Dark: `bg-white/5 border-white/10`
    - Light: `bg-white/40 border-slate-200/50 hover:bg-white/60`
  - `.glass-input`:
    - Dark: `bg-slate-950/50 border-white/10 text-slate-200`
    - Light: `bg-white/50 border-slate-200 text-slate-900 placeholder-slate-400`

## 3. 实现主题逻辑与 UI 适配 (src/App.tsx)
- **状态管理**：
  - 添加 `theme` 状态 ('light' | 'dark')。
  - 初始化时读取 `localStorage` 或系统偏好。
  - 切换时同步修改 `document.documentElement` 的 `class` 并持久化。
- **UI 适配**：
  - **Header**：添加 ☀️/🌙 切换按钮。
  - **Sidebar**：背景从 `bg-slate-900/20` 改为 `bg-white/20 dark:bg-slate-900/20`。
  - **Text**：将 `text-slate-100/200` 替换为 `text-slate-900 dark:text-slate-100` 等自适应颜色。
  - **Icons**：调整图标颜色以适应亮色背景。

## 4. 适配历史记录组件 (src/HistoryView.tsx)
- **模态框 (Modal)**：
  - 背景：`bg-white dark:bg-slate-900`。
  - 边框：`border-slate-200 dark:border-white/10`。
- **卡片 (Card)**：
  - 背景：`bg-white/50 dark:bg-white/5`。
  - 文字：适配深浅色。

## 5. 验证
- 确认点击切换按钮能即时切换主题。
- 确认刷新页面后主题状态保留。
- 检查亮色模式下 Glassmorphism 效果是否清晰可见且美观。
