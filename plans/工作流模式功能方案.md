# 工作流模式功能设计方案

## 需求描述

工作流模式是预设了提示词的快捷生图模式，用户只需：
1. 选择预设的工作流模板
2. 上传参考图片
3. 选择宽高比和分辨率
4. 点击生成

## 核心概念

**工作流模板**：预定义的提示词模板，针对特定场景优化，例如：
- 🎨 艺术风格转换
- 📸 照片增强
- 🖼️ 背景替换
- ✨ 特效添加
- 🎭 风格迁移

## UI 布局

```
┌─────────────────────────────────────────────┐
│  Logo  [生图模式] [工作流模式]      ⚙️ 设置  │
├──────────────┬──────────────────────────────┤
│              │                              │
│  左侧面板    │    右侧面板                  │
│              │                              │
│  工作流选择  │    结果预览                  │
│  ┌────────┐ │    ┌──────────────────┐     │
│  │ 模板1  │ │    │                  │     │
│  │ 模板2  │ │    │   生成的图片     │     │
│  │ 模板3  │ │    │                  │     │
│  └────────┘ │    └──────────────────┘     │
│              │                              │
│  上传图片    │    图片信息                  │
│  宽高比      │    下载按钮                  │
│  分辨率      │                              │
│  生成按钮    │                              │
│              │                              │
└──────────────┴──────────────────────────────┘
```

## 数据结构

### 工作流模板类型

```typescript
type WorkflowTemplate = {
  id: string
  name: string
  icon: string
  description: string
  prompt: string
  category: 'art' | 'photo' | 'effect' | 'style'
}
```

### 预设模板示例

```typescript
const WORKFLOW_TEMPLATES: WorkflowTemplate[] = [
  {
    id: 'anime-style',
    name: '动漫风格',
    icon: '🎨',
    description: '将照片转换为动漫风格',
    prompt: 'Convert this image to anime style, with vibrant colors, clean lines, and typical anime character features',
    category: 'art'
  },
  {
    id: 'oil-painting',
    name: '油画风格',
    icon: '🖼️',
    description: '转换为油画艺术风格',
    prompt: 'Transform this image into an oil painting style with visible brush strokes, rich textures, and artistic color blending',
    category: 'art'
  },
  {
    id: 'photo-enhance',
    name: '照片增强',
    icon: '📸',
    description: '提升照片质量和细节',
    prompt: 'Enhance this photo with improved clarity, better lighting, enhanced colors, and sharper details while maintaining natural look',
    category: 'photo'
  },
  {
    id: 'background-blur',
    name: '背景虚化',
    icon: '🌫️',
    description: '虚化背景突出主体',
    prompt: 'Apply professional bokeh effect to blur the background while keeping the main subject sharp and in focus',
    category: 'effect'
  },
  {
    id: 'vintage-film',
    name: '复古胶片',
    icon: '📷',
    description: '复古胶片相机效果',
    prompt: 'Apply vintage film camera effect with grain, faded colors, light leaks, and nostalgic atmosphere',
    category: 'style'
  },
  {
    id: 'cyberpunk',
    name: '赛博朋克',
    icon: '🌃',
    description: '赛博朋克未来风格',
    prompt: 'Transform into cyberpunk style with neon lights, futuristic elements, dark atmosphere, and high-tech aesthetic',
    category: 'style'
  }
]
```

## 实现步骤

### 1. 添加工作流状态

```typescript
const [selectedWorkflow, setSelectedWorkflow] = useState<WorkflowTemplate | null>(null)
```

### 2. 工作流模式布局

```tsx
{mode === 'workflow' ? (
  <>
    <section className="leftPanel">
      <div className="cardTitle">工作流参数</div>
      
      {/* 工作流选择 */}
      <div className="field">
        <div className="label">选择工作流模板</div>
        <div className="workflowGrid">
          {WORKFLOW_TEMPLATES.map(template => (
            <div
              key={template.id}
              className={`workflowCard ${selectedWorkflow?.id === template.id ? 'active' : ''}`}
              onClick={() => setSelectedWorkflow(template)}
            >
              <div className="workflowIcon">{template.icon}</div>
              <div className="workflowName">{template.name}</div>
              <div className="workflowDesc">{template.description}</div>
            </div>
          ))}
        </div>
      </div>
      
      {/* 图片上传（复用生图模式的组件）*/}
      <ImageUpload />
      
      {/* 宽高比和分辨率（复用生图模式的组件）*/}
      <AspectRatioSelector />
      <ResolutionSelector />
      
      {/* 生成按钮 */}
      <GenerateButton disabled={!selectedWorkflow || inputImages.length === 0} />
    </section>
    
    <section className="rightPanel">
      {/* 结果预览（复用生图模式的预览组件）*/}
      <ResultPreview />
    </section>
  </>
) : null}
```

### 3. 生成逻辑

```typescript
async function onWorkflowGenerate() {
  if (!selectedWorkflow) return
  
  // 使用工作流模板的提示词
  const img = await generateImage({
    apiBaseUrl,
    apiPath,
    apiKey,
    authHeader,
    prompt: selectedWorkflow.prompt, // 使用预设提示词
    aspectRatio,
    imageSize: imageSize.imageSize,
    inputImages: inputImages.map(img => ({
      mimeType: img.mimeType,
      base64Data: img.base64Data
    }))
  })
  
  // 处理结果...
}
```

## CSS 样式

```css
/* 工作流选择器 */
.workflowGrid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(140px, 1fr));
  gap: 12px;
  margin-top: 12px;
}

.workflowCard {
  padding: 16px;
  border-radius: 12px;
  border: 1px solid var(--border);
  background: rgba(255, 255, 255, 0.04);
  cursor: pointer;
  transition: all 0.2s;
  text-align: center;
}

.workflowCard:hover {
  background: rgba(255, 255, 255, 0.08);
  transform: translateY(-2px);
}

.workflowCard.active {
  border-color: rgba(124, 58, 237, 0.6);
  background: rgba(124, 58, 237, 0.15);
}

.workflowIcon {
  font-size: 32px;
  margin-bottom: 8px;
}

.workflowName {
  font-size: 14px;
  font-weight: 600;
  color: var(--text);
  margin-bottom: 4px;
}

.workflowDesc {
  font-size: 12px;
  color: var(--muted);
  line-height: 1.4;
}
```

## 功能特性

✅ **预设模板**：6+ 个常用工作流模板  
✅ **快速生成**：无需手动输入提示词  
✅ **视觉选择**：卡片式模板选择界面  
✅ **复用组件**：共享图片上传、预览等组件  

## 与生图模式的区别

| 特性 | 生图模式 | 工作流模式 |
|------|---------|-----------|
| 提示词 | 用户手动输入 | 预设模板 |
| 参考图片 | 可选 | 必需 |
| 使用场景 | 自由创作 | 快速处理 |
| 学习成本 | 需要了解提示词 | 零学习成本 |

## 总结

工作流模式通过预设提示词模板，降低了 AI 图片生成的使用门槛，让用户无需学习复杂的提示词技巧，即可快速获得专业效果。
